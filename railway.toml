[build]
builder = "nixpacks"
watchPatterns = ["*.py", "*.yaml", "*.toml", "*.txt", "start.sh", "Dockerfile*"]

[deploy]
startCommand = "sh start.sh"
restartPolicyType = "always"        # auto-restart if the bot crashes
restartPolicyMaxRetries = 10
